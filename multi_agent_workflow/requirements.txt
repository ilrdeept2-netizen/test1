# 멀티 에이전트 워크플로우 의존성
# Claude Agent SDK 기반

# Core - Claude API
anthropic>=0.39.0

# Agent SDK (설치 시 필요)
# pip install anthropic-agent-sdk
# 또는 npm install @anthropic-ai/claude-agent-sdk (TypeScript용)

# 문서 처리
python-docx>=1.1.0
PyPDF2>=3.0.0
olefile>=0.47
lxml>=5.0.0

# 웹 프레임워크
flask>=3.0.0
flask-cors>=4.0.0
streamlit>=1.30.0

# 뉴스 수집
feedparser>=6.0.0
beautifulsoup4>=4.12.0
requests>=2.31.0

# 데이터 처리
pyyaml>=6.0.0
pydantic>=2.5.0

# 스케줄링
apscheduler>=3.10.0

# 비동기 처리
aiohttp>=3.9.0
asyncio-throttle>=1.0.0

# 유틸리티
python-dotenv>=1.0.0
tqdm>=4.66.0

# 개발/테스트
pytest>=7.4.0
pytest-asyncio>=0.23.0

# 선택사항 (한국어 처리)
# konlpy>=0.6.0
# scikit-learn>=1.3.0
