# 2개 Claude 계정 — GitHub을 공유 데이터베이스로 활용하기

2개의 서로 다른 Claude 계정이 **동일한 GitHub 레포지토리**에 연동되어 작업할 때, GitHub 자체가 사실상 공유 데이터베이스 역할을 한다는 점을 분석한 문서입니다.

---

## GitHub에 남는 공유 가능한 정보들

### 1. Git 히스토리 (완전 공유)
- **커밋 로그**: 두 계정이 만든 모든 커밋이 하나의 히스토리에 합쳐짐
- **브랜치 목록**: 모든 브랜치가 양쪽에서 보임
- **태그/릴리즈**: 공유
- **diff/blame**: 누가 어떤 줄을 언제 바꿨는지 추적 가능

### 2. GitHub Issues & PRs (사실상 데이터베이스)
- **PR 내역**: 코드 리뷰, 코멘트, 승인/거절 기록
- **Issue 트래커**: 버그, 기능 요청, 토론
- **Labels/Milestones**: 프로젝트 관리 메타데이터
- **Linked Issues**: PR ↔ Issue 연결 관계

### 3. GitHub Actions / CI 로그
- 빌드/테스트 결과가 계정과 무관하게 레포에 남음
- 워크플로우 실행 이력 = 프로젝트 상태 히스토리

### 4. Wiki & Discussions
- 레포 Wiki는 별도 git repo이지만 같은 공간에서 공유
- Discussions는 포럼처럼 지식이 축적됨

---

## 핵심 포인트: Claude Code의 로컬 이력은 분리됨

```
계정 A (Claude Code)          계정 B (Claude Code)
┌─────────────────┐          ┌─────────────────┐
│ 로컬 대화 이력    │          │ 로컬 대화 이력    │
│ ~/.claude/       │          │ ~/.claude/       │
│ (완전 분리)       │          │ (완전 분리)       │
└────────┬────────┘          └────────┬────────┘
         │ push/PR                    │ push/PR
         ▼                            ▼
┌──────────────────────────────────────────────┐
│              GitHub Repository                │
│                                              │
│  ✅ 커밋 히스토리      (공유)                  │
│  ✅ PR & 코드리뷰      (공유)                  │
│  ✅ Issues & 댓글      (공유)                  │
│  ✅ Branch 목록        (공유)                  │
│  ✅ CI/CD 로그         (공유)                  │
│  ✅ Wiki & Discussions (공유)                  │
│  ✅ 파일 내용 & blame  (공유)                  │
│  ✅ Release & Tags     (공유)                  │
│                                              │
│  ❌ Claude 대화 컨텍스트  (저장 안 됨)          │
│  ❌ 로컬 .claude/ 설정    (저장 안 됨)          │
│  ❌ MCP 서버 런타임 상태  (저장 안 됨)          │
└──────────────────────────────────────────────┘
```

---

## 실질적으로 "데이터베이스처럼" 활용하는 방법

### 방법 1: CLAUDE.md를 프로젝트 메모리로 사용

```markdown
# CLAUDE.md (레포 루트에 커밋)
## 프로젝트 컨벤션
- 테스트는 pytest로 실행
- API 키는 .env에 보관

## 최근 결정사항
- 2026-03-01: 멀티계정 연동 분석 완료
- 2026-02-28: PVP 분석 문서 추가
```

이 파일은 두 계정 어디서 Claude Code를 실행하든 **자동으로 읽혀서** 컨텍스트가 공유됩니다.

### 방법 2: PR 코멘트에 의사결정 기록 남기기
- 왜 이 방식을 선택했는지
- 대안은 뭐였는지
- 이런 내용이 GitHub에 영구 저장됨

### 방법 3: GitHub Projects (칸반 보드)
- Issue 카드를 칸반 보드에 정리
- 두 계정 모두 같은 보드를 보며 작업 상태 파악

---

## 결론

GitHub 자체가 **커밋, PR, Issue, Wiki** 등을 통해 상당히 풍부한 공유 데이터베이스 역할을 합니다. Claude Code의 로컬 대화 이력만 분리될 뿐, 프로젝트의 **코드, 의사결정, 작업 흐름**은 모두 GitHub에 남아서 어느 계정에서든 접근 가능합니다.

특히 **CLAUDE.md 파일을 레포에 커밋**해두면, 어떤 계정으로 접속하든 Claude Code가 프로젝트 컨텍스트를 자동으로 이해하게 되어 사실상 "공유 메모리" 역할을 합니다.
