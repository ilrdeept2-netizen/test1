# 옵시디언 × Claude Code: 토큰 낭비 없는 나만의 지식 베이스

> 작성일: 2026-02-27

---

## 핵심 요약

| 방법 | 토큰 소비 | 구현 난이도 | 결론 |
|------|----------|------------|------|
| MCP 노트앱 연동 | 매 턴마다 낭비 | 높음 | 비추천 |
| 옵시디언 MCP 설치 | 여전히 낭비 | 중간 | 비추천 |
| **옵시디언 + settings 2줄** | **최소화** | **낮음** | **추천** |

---

## 왜 MCP 방식은 토큰 낭비인가?

MCP(Model Context Protocol)로 노트앱을 연동하면, 연결된 서버가 **매 턴마다 컨텍스트에 자동으로 포함**된다.
Claude가 실제로 노트가 필요한지와 관계없이 토큰이 소모된다.

```
[매 턴 발생]
사용자 메시지
+ MCP 서버 컨텍스트 (노트 목록, 메타데이터 등)  ← 불필요한 토큰
+ Claude 응답
```

---

## 올바른 방법: 마크다운 + 로컬 파일시스템

### 원리

```
옵시디언 볼트 (~/Documents/Obsidian/)
    └── note1.md
    └── note2.md
    └── 프로젝트/
        └── idea.md

Claude Code가 필요할 때만 직접 읽기 → 토큰 소비 없음
```

Claude Code는 로컬 파일시스템에 직접 접근할 수 있다.
MCP 없이도 `Read`, `Glob`, `Grep` 도구로 마크다운 파일을 읽을 수 있다.

---

## 설정 방법 (2단계)

### 1단계: `~/.claude/settings.json`에 한 줄 추가

```json
{
  "additionalDirectories": ["/Users/나/Documents/Obsidian"]
}
```

`additionalDirectories` = 어떤 프로젝트에서 실행해도 이 경로에 접근 허용.
이 설정 없이는 Claude Code가 현재 프로젝트 폴더 밖을 읽을 수 없다.

### 2단계: `~/.claude/CLAUDE.md`에 볼트 경로와 폴더 구조 기재

```markdown
## Personal Knowledge Base
볼트 경로: /Users/나/Documents/Obsidian/

폴더 구조:
- 나/         → 개인 설정, 선호, 자주 쓰는 정보
- 프로젝트/   → 진행 중인 프로젝트별 컨텍스트
- Claude/     → 저장된 프롬프트, 작업 패턴
- 일기/       → 날짜별 메모

"옵시디언에 저장해줘" 요청 시 적절한 폴더에 마크다운으로 저장할 것.
```

이게 전부다. Claude Code는 이제:
- 어디에 지식 베이스가 있는지 안다
- **필요할 때만** 파일을 읽는다 (매 턴 자동 포함 X)
- MCP 오버헤드 없이 직접 파일시스템에 접근한다
- **어떤 프로젝트에서 열어도** "옵시디언에 저장해줘"가 바로 동작한다

---

## 옵시디언을 추천하는 이유

1. **마크다운 네이티브** — Claude가 직접 읽고 쓸 수 있는 형식
2. **로컬 파일 저장** — MCP 없이 파일시스템으로 접근 가능
3. **링크 구조** — `[[노트이름]]` 형식으로 연결, Glob으로 탐색 용이
4. **무료 + 오프라인** — 서버 불필요, 데이터 로컬 보관
5. **AI 에이전트 친화적** — 텍스트 파일이므로 Claude가 읽기/검색/수정 가능

---

## 활용 패턴

### 패턴 1: 프로젝트별 컨텍스트 주입

```markdown
<!-- ~/Documents/Obsidian/프로젝트/특허업무.md -->
# 특허 업무 컨텍스트

- 담당 클라이언트: A사, B사
- 자주 쓰는 IPC 코드: G06F, H04L
- 제출 마감: 매월 15일
```

Claude에게: "특허업무 노트 보고 오늘 할 일 정리해줘"
→ Claude가 해당 파일만 읽고 응답 (다른 노트 불필요)

### 패턴 2: 반복 프롬프트 저장

```markdown
<!-- ~/Documents/Obsidian/Claude/자주쓰는프롬프트.md -->
# 코드 리뷰 요청
"다음 코드를 리뷰해줘. 보안 취약점, 성능 문제, 가독성 순으로 피드백 줘."

# 문서 작성 요청
"이 내용을 팀원이 읽을 수 있도록 한국어로 명확하게 정리해줘."
```

### 패턴 3: 개인 정보/선호 저장

```markdown
<!-- ~/Documents/Obsidian/나/설정.md -->
- 코딩 스타일: 함수형 선호, 타입힌트 필수
- 작업 환경: macOS + VSCode + Python 3.11
- 금지사항: 긴 설명 금지, 코드 먼저
```

---

## 전체 설정 예시

### `~/.claude/settings.json`

```json
{
  "additionalDirectories": ["/Users/나/Documents/Obsidian"]
}
```

### `~/.claude/CLAUDE.md`

```markdown
# Claude Code 전역 설정

## Personal Knowledge Base
볼트 경로: /Users/나/Documents/Obsidian/

폴더 구조:
- 나/설정.md        → 코딩 스타일, 환경, 선호사항
- 프로젝트/         → 프로젝트별 컨텍스트 및 메모
- Claude/프롬프트/  → 자주 쓰는 프롬프트 패턴

저장 요청 시 적절한 폴더 판단하여 마크다운으로 저장.

## 작업 원칙
- 코드 먼저, 설명은 최소화
- 커밋 전 항상 테스트 실행
```

---

## 비교 정리

```
[MCP 방식]
매 턴: 사용자 메시지 + MCP 서버 데이터 + Claude 응답
토큰: 많음 / 비용: 높음 / 설정: 복잡

[로컬 파일시스템 방식]
매 턴: 사용자 메시지 + Claude 응답
필요시: + 특정 파일 내용 (요청 시에만)
토큰: 최소 / 비용: 낮음 / 설정: 2줄
```

---

## 결론

마크다운 + 로컬 파일시스템 = **AI 에이전트가 직접 접근 가능한 나만의 지식 베이스**

옵시디언은 이 용도에 가장 적합한 도구다.
MCP 설치 없이, `~/.claude/CLAUDE.md`에 2줄 추가하는 것만으로 동일한 기능을 토큰 낭비 없이 구현할 수 있다.
